print.h
-------

Generic print()/fprint()/sprint() functions
inspired by high-level languages.

All code is in single header.

[ EXAMPLE ]
	print("float: ", 1.5F);

	char *value = "VALUE";
	fprint(fp, "VARIABLE=", value);

	char buf[12];
	sprint(buf, sizeof(buf), "Hello world!");


[ INSTALLATION ]
	# cp print.h /usr/include
